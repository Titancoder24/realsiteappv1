Also, the dashboard looks very empty. The other projects inside the dashboard look very empty too. I'd like charts and other things. Every data needs to come from the dashboard or that Supabase. Also, they have all the data, which needs some sort of chart to use the pre-existing version of the thing, or Garanda.